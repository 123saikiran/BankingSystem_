"# BankingSystem_

This repository contains a Spring Boot backend for a banking websocket application using an in-memory H2 database and a React frontend scaffold under `frontend/`.

See `FRONTEND_INSTRUCTIONS.md` for frontend setup and integration details.
" 
